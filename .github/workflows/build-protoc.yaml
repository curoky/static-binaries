# Autogenerated file.
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#  @generated
 
name: build protoc
on:
  push:
    paths:
      - 'Formula/protoc@*'
      - '.github/workflows/build-protoc.yaml'

jobs:
  install:
    strategy:
      fail-fast: false
      max-parallel: 20
      matrix:
        os: [ubuntu20.04]
        version:
          - '3.1.0'
          - '3.2.1'
          - '3.3.2'
          - '3.4.1'
          - '3.5.2'
          - '3.7.1'
          - '3.8.0'
          - '3.9.2'
          - '3.13.0.1'
          - '3.14.0'
          - '3.15.6'

    runs-on: ubuntu-latest
    container:
      image: homebrew/${{ matrix.os }}

    env:
      HOMEBREW_NO_ANALYTICS: 1
      HOMEBREW_NO_AUTO_UPDATE: 1

    steps:
      - uses: actions/checkout@v2
      - run: brew tap curoky/tap
      - name: Install and test
        uses: curoky/actions/brew-tap-install-test@master
        with:
          formula: protoc@${{ matrix.version }}
          upgrade: true
          install_args: "--verbose"
          test_args: "--verbose"

      - name: update binary to release
        uses: svenstaro/upload-release-action@2.2.1
        with:
          tag: binary
          overwrite: true
          file: /home/linuxbrew/.linuxbrew/opt/protoc@${{ matrix.version }}/bin/protoc
          asset_name: protoc-${{ matrix.version }}
          repo_token: ${{ secrets.GH_UPLOAD_KEY }}
