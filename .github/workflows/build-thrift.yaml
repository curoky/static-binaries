# Autogenerated file.
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#  @generated
 
name: build thrift
on:
  push:
    paths:
      - 'Formula/thrift@*'
      - '.github/workflows/build-thrift.yaml'

jobs:
  install:
    strategy:
      fail-fast: false
      max-parallel: 20
      matrix:
        os: [ubuntu20.04]
        version:
          - '0.8.0'
          - '0.9.0'
          - '0.9.1'
          - '0.9.2'
          - '0.9.3'
          - '0.9.3.1'
          - '0.10.0'
          - '0.11.0'
          - '0.12.0'
          - '0.13.0'
          - '0.14.1'

    runs-on: ubuntu-latest
    container:
      image: homebrew/${{ matrix.os }}

    env:
      HOMEBREW_NO_ANALYTICS: 1
      HOMEBREW_NO_AUTO_UPDATE: 1

    steps:
      - uses: actions/checkout@v2
      - run: brew tap curoky/tap
      - name: Install and test
        uses: curoky/actions/brew-tap-install-test@master
        with:
          formula: thrift@${{ matrix.version }}
          upgrade: true
          install_args: "--verbose"
          test_args: "--verbose"

      - name: update binary to release
        uses: svenstaro/upload-release-action@2.2.1
        with:
          tag: binary
          overwrite: true
          file: /home/linuxbrew/.linuxbrew/opt/thrift@${{ matrix.version }}/bin/thrift
          asset_name: thrift-${{ matrix.version }}
          repo_token: ${{ secrets.GH_UPLOAD_KEY }}
