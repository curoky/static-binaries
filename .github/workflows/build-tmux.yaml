# Autogenerated file.
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#  @generated
 
name: build tmux
on:
  push:
    paths:
      - 'Formula/tmux@*'
      - '.github/workflows/build-tmux.yaml'

jobs:
  install:
    strategy:
      fail-fast: false
      max-parallel: 20
      matrix:
        os: [ubuntu20.04]
        version:
          - '3.1'
          - '3.0'
          - '2.9'
          - '2.8'
          - '2.7'
          - '2.6'
          - '2.5'

    runs-on: ubuntu-latest
    container:
      image: homebrew/${{ matrix.os }}

    env:
      HOMEBREW_NO_ANALYTICS: 1
      HOMEBREW_NO_AUTO_UPDATE: 1

    steps:
      - uses: actions/checkout@v2
      - run: brew tap curoky/tap
      - name: Install and test
        uses: curoky/actions/brew-tap-install-test@master
        with:
          formula: tmux@${{ matrix.version }}
          upgrade: true
          install_args: "--verbose"
          test_args: "--verbose"

      - name: update binary to release
        uses: svenstaro/upload-release-action@2.2.1
        with:
          tag: binary
          overwrite: true
          file: /home/linuxbrew/.linuxbrew/opt/tmux@${{ matrix.version }}/bin/tmux
          asset_name: tmux-${{ matrix.version }}
          repo_token: ${{ secrets.GH_UPLOAD_KEY }}
