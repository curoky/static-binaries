# Autogenerated file.
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#  @generated
 
name: build fbthrift
on:
  push:
    paths:
      - 'Formula/fbthrift@*'
      - '.github/workflows/build-fbthrift.yaml'

jobs:
  install:
    strategy:
      fail-fast: false
      max-parallel: 20
      matrix:
        os: [ubuntu20.04]
        version:
          - '2019.06.03'
          - '2019.12.30'
          - '2020.12.14'
          - '2021.03.01'

    runs-on: ubuntu-latest
    container:
      image: homebrew/${{ matrix.os }}

    env:
      HOMEBREW_NO_ANALYTICS: 1
      HOMEBREW_NO_AUTO_UPDATE: 1

    steps:
      - uses: actions/checkout@v2
      - run: brew tap curoky/tap
      - name: Install and test
        uses: curoky/actions/brew-tap-install-test@master
        with:
          formula: fbthrift@${{ matrix.version }}
          upgrade: true
          install_args: "--verbose"
          test_args: "--verbose"

      - name: update binary to release
        uses: svenstaro/upload-release-action@2.2.1
        with:
          tag: binary
          overwrite: true
          file: /home/linuxbrew/.linuxbrew/opt/fbthrift@${{ matrix.version }}/bin/thrift1
          asset_name: thrift1-${{ matrix.version }}
          repo_token: ${{ secrets.GH_UPLOAD_KEY }}
