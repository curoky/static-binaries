name: build sbt

# Builds the sbt client itself (tools/sbt, Go) and publishes it to the same
# ghcr registry as the packages, so it can be bootstrapped with a single curl
# and later self-upgraded like any other package.

on:
  push:
    paths:
      - '.github/workflows/build-sbt.yaml'
      - 'tools/sbt/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - { goos: linux, goarch: amd64, tag: linux-x86_64 }
          - { goos: darwin, goarch: arm64, tag: darwin-arm64 }
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache-dependency-path: tools/sbt/go.sum

      # Statically-linked binary with no host runtime dependencies (CGO off).
      - working-directory: tools/sbt
        env:
          CGO_ENABLED: '0'
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          go vet ./...
          go test ./...
          mkdir -p ./sbt
          go build -trimpath -ldflags="-s -w" -o ./sbt/sbt .
          tar -czvf ./sbt.${{ matrix.tag }}.tar.gz ./sbt

      - uses: oras-project/setup-oras@v1

      - run: echo "${{ secrets.GITHUB_TOKEN }}" | oras login ghcr.io -u ${{ github.actor }} --password-stdin

      - working-directory: tools/sbt
        run: >
          oras push ghcr.io/curoky/static-binaries-v4:sbt-${{ matrix.tag }}
          ./sbt.${{ matrix.tag }}.tar.gz:application/vnd.oci.image.layer.v1.tar+gzip
